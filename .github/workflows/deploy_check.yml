name: Deploy Check

on:
  workflow_dispatch:

jobs:
  deploy-check-disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Deployment checks disabled in public repo
        run: echo "Deployment checks now run from private release workflows only."