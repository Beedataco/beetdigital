name: Publish Chatwoot EE docker images

on:
  workflow_dispatch:

jobs:
  publish-disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Publishing disabled in public repo
        run: echo "EE publishing moved to private release workflows."