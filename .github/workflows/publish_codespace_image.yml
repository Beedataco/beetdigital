name: Publish Codespace Base Image

on:
  workflow_dispatch:

jobs:
  publish-disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Publishing disabled in public repo
        run: echo "Codespace image publishing moved to private release workflows."